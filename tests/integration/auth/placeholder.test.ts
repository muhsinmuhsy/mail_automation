import { describe, it, expect } from 'vitest';

describe('integration/auth', () => {
  it('placeholder — auth tests are in tests/unit/auth/ and tests/integration/api/auth-guards.test.ts', () => {
    expect(true).toBe(true);
  });
});
